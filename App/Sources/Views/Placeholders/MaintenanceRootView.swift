import SwiftUI

/// Placeholder for the Maintenance section — replaced by T17
/// (`Views/Maintenance/`). `MainWindow` routes to this name.
struct MaintenanceRootView: View {
    var body: some View {
        PlaceholderDetailView(
            title: "Maintenance",
            symbolName: "wrench.and.screwdriver",
            message: "Repository size, retention, integrity checks and staleness will appear here."
        )
    }
}
