import SwiftUI

/// Placeholder for the Runs section — replaced by T15
/// (`Views/Runs/`). `MainWindow` routes to this name.
struct RunsRootView: View {
    var body: some View {
        PlaceholderDetailView(
            title: "Runs",
            symbolName: "clock.arrow.circlepath",
            message: "Backup history and logs will appear here."
        )
    }
}
